management:
  server:
    port: 8080
  prometheus:
    metrics.export.enabled: false

spring:
  flyway:
    enabled: false

clients:
  services:
    products:
      base-url: http://shopping-assistant
    orders:
      base-url: http://shopping-assistant
